import { createMuiTheme } from "@material-ui/core/styles";

const WHITE = "#FFF";
const PINK = "#FF385C";
const GREY = "#717171";
const BLACK = "#222222";
const WHITEDARKER = "#E5E5E5";
const theme = createMuiTheme({
  myColor: {
    WHITE,
    PINK,
    GREY,
    BLACK,
    WHITEDARKER,
  },
});

export default theme;
